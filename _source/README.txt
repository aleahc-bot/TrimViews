_source/ — working files. DO NOT UPLOAD to the web server.

manifest.tsv  Photo inventory: category, filename, project/client slug, width,
              height, orientation. Used to organise the gallery images. It is
              kept out of the web root because the third column contains
              client names.
